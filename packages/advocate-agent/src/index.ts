export type {
  AdvocateConfig,
  AdvocateFacts,
  AdvocateFactItem,
  AdvocateToolFlags,
  AdvocateCapabilities,
} from "./config";

// UI surface (AdvocateChat, ChatLauncher) is added in the agent-package phase.
